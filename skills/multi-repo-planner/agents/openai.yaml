interface:
  display_name: "Multi Repo Planner"
  short_description: "Coordinate canonical CENTRAL tasks across active repos with status discipline."
  default_prompt: "Plan and dispatch canonical CENTRAL tasks across repositories, then update CENTRAL first and any repo-local mirrors second after worker completion."
