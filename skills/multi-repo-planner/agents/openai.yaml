interface:
  display_name: "Multi Repo Planner"
  short_description: "Coordinate Txx tasks across active repos with status discipline."
  default_prompt: "Plan and dispatch Txx tasks across repositories, then enforce tasks.md status updates after worker completion."
